version: "3.9"

services:
  zypryx-api:
    build:
      context: ../ZypryxAPI/ZypryxAPI
      dockerfile: Dockerfile
    container_name: zypryx-api
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: "Production"
    restart: always
